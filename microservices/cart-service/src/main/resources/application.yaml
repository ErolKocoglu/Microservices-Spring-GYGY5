spring:
  config:
    import: "optional:configserver:http://localhost:8888"
  application:
    name: cart-service

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: "*"
