spring:
    config:
        import: "optional:configserver:http://localhost:8888"
    application:
        name: notification-service

server:
    port: 8085

management:
  endpoints:
    web:
      exposure:
        include: "*"
