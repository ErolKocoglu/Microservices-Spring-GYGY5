spring:
    config:
        import: "optional:configserver:http://localhost:8888"
    application:
        name: order-service

server:
    port: 8084

management:
  endpoints:
    web:
      exposure:
        include: "*"
